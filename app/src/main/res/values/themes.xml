<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base App Theme using AppCompat -->
    <style name="Base.Theme.FarmStayz" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/green_accent</item>
        <item name="colorAccent">@color/green_accent</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:buttonStyle">@style/Widget.AppCompat.Button</item>
        <item name="android:editTextBackground">@android:color/transparent</item>
    </style>

    <style name="Theme.FarmStayz" parent="Base.Theme.FarmStayz" />

</resources>
